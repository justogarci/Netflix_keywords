// Inicializar la aplicación
document.addEventListener('DOMContentLoaded', () => {
    indexContr();      // Cargar vista inicial
    setupRouter();     // Configurar el router
});
console.log("INICIADO");